package CoreJavaConceptsForSelenium.PART_4.Collections.Maps;

public class Maps_Introduction {

    /// Map is core part in collection framework but its also represents a data structure
    /// Maps stores data in key value pair
    /// Key must be unique, Values can be duplicate
    /// Map is an interface

    /**
     * 4 types of Maps
     * HashMap -> Stores Key Value pair in a hash Table, doesn't guarantee order of elements
     * LinkedHashMap  ->  Similar performance as hashMap, +1 as maintain insertion Order
     * TreeMap -> Stores key Value pair in Sorted Order
     * ConcurrentHashMap -> thread safe implementation of Map for concurrent environment
     */
}

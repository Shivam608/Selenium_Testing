package CoreJavaConceptsForSelenium.PART_4.Collections.Set;

public class Set_Introduction {

    ///  Set is generally an un-ordered collection of data
    ///  Set does not allow duplicate elements
    ///  Set is an Interface

    /**
     * Set is of 3 Types
     *  Hashset -> Fast performance, doesn't guarantee Order of Insertion, at max allows 1 null element
     *  LinkedHashSet -> Maintains Order of Insertion, at max allows 1 null element
     *  TreeSet -> Maintains sorted order, doesn't allow null elements, at max allows 0 null element (throws NullPointerExecution)
     */

    ///  LinkedHashMap uses LinkedList mechanism internally to maintain the insertion Order
}
